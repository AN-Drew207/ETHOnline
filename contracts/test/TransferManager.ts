import hre, {waffle} from "hardhat";

//import {approveFlow} from "@utils/superfluid";
//import {superfluidFixture} from "./fixtures";

const {loadFixture} = waffle;

describe("TransferManager", () => {
  it("Should create flows from approved sources", async () => {
    //const {superfluid, signer, accounts} = await loadFixture(superfluidFixture);
    //await approveFlow(hre,{sender:signer,receiver:})
  });
});
